import 'package:flutter/material.dart';

class HabilidadesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Habilidades Tab"),
    );
  }
}
