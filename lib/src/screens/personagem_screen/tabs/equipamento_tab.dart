import 'package:flutter/material.dart';

class EquipamentoTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Equipamento Tab"),
    );
  }
}
